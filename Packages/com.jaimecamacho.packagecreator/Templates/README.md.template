# __DISPLAY_NAME__

Unity package `__PACKAGE_NAME__`.

## Installation

Add the package by name in the Unity Package Manager or from a scoped registry that exposes `__PACKAGE_NAME__`.

## Contents

- `Runtime/` runtime code.
- `Editor/` editor-only tools.
- `Documentation~/` package documentation.
- `Samples~/` optional importable samples.

## Development

Update `package.json`, `CHANGELOG.md` and `README.md` before creating a release.
